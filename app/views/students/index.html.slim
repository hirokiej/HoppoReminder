- set_meta_tags title: '生徒のカレンダー登録名を設定', description: '生徒のカレンダー登録名を設定するページです。'

h1.text-xl.py-4.font-bold
  | 生徒のカレンダー登録名を設定
p.pb-2
  | 登録した名前が「レッスン変更」に表示されます

- if current_user.students.present?
  = form_with model: current_user,
              url: bulk_update_students_path,
              method: :patch do |f|
    table.container.mx-auto
      thead
        tr
          th.pb-2 LINEの表示名
          th.pb-2 カレンダーの登録名
      tbody
        - current_user.students.each do |student|
          = f.fields_for :students, student do |s|
            tr
              td
                = student.line_display_name
              td
                = s.hidden_field :id
                = s.text_field :real_name, class: 'border border-gray-500'
    .flex.justify-end.pt-3.mr-10
      = f.submit '更新', class: 'text-white font-bold bg-blue-600 rounded-2xl px-5 px-2 hover:bg-blue-700'
- else
  p.text-sm
  | 本サービス登録後に生徒からトークを受信すると生徒が表示されます。
