- if current_user
  .flex.items-center.justify-center
    = link_to root_path do
      = image_tag 'logo_long.svg', class: 'h-14 w-auto'
  - if has_line_info?(current_user)
    = render 'shared/tab_menu', tabs: navigation_tabs
